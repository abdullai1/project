import React from 'react'
import MapComponent from './MapComponent'

const MapWrapper = () => {
    return <div>
        <div>The next step is to load Programs from Organization Units and use those to list clusters</div>
        <MapComponent/>
    </div>
}
export default MapWrapper;